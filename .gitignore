# Dependencies
node_modules
.pnpm-store
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.development
.env.production

# Secrets
secrets
infisical/secrets

# Turbo (leftover from monorepo tooling, harmless if unused)
.turbo

# Build outputs
build
dist
.test-build

# Backup dumps (HW #15) — real row data, regenerated by scripts/backup.sh
backups

# Backup dumps from the pgbackups sidecar (HW #15, opt-in profile) — separate
# mechanism, separate directory, same reasoning as backups/ above
pgbackups

# Generated OpenAPI artifacts
openapi/docs.html
spec.json

# Generated Pact contracts (HW #16) — regenerated by `npm run test:contract`,
# see README's Testing section for why this isn't committed
pacts

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem

# TypeScript
*.tsbuildinfo
